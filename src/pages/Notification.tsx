import NotificationHeader from "@/components/notification/NotificationHeader";

export default function NotificationPage() {
  return (
    <div className="mx-auto w-full h-full pt-2 bg-white max-w-md shadow-sm flex flex-col justify-between flex flex-col gap-2 w-full min-h-screen">
      <NotificationHeader />
    </div>
  );
}
