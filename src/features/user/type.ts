export type User = {
  name: string;
  job: string;
  joinDate: string;
  officeLocation: string;
  image: string;
};

export type OnlineUser = {
  name: string;
  location: string;
  avatar: string;
};
